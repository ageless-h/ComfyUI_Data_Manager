# 运行时依赖
aiohttp>=3.8.0
Pillow>=9.0.0
markdown>=3.4.0
send2trash>=1.8.0  # 安全删除文件到回收站

# 开发依赖
pytest>=7.0.0
playwright>=1.40.0

# 可选依赖（用于增强功能）
mammoth>=1.6.0  # Word 文档 (.doc/.docx) 预览支持
openpyxl>=3.0.0  # Excel 表格 (.xlsx) 预览支持
paramiko>=2.12.0  # SSH/SFTP 远程文件访问支持
